{
  "name": "minimusiclab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "pnpm build && gh-pages -d dist"
  },
  "dependencies": {
    "tone": "^14.7.77"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.0"
  }
}
